name: Deploy AdPanel

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Pull & Build
        run: |
          cd /opt/adpanel
          git pull origin main
          go build -o adpanel .
          sudo systemctl restart adpanel
